<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  </head>
</head>
    <body class="container-fluid text-center">
    <div class="row p-4">
        <div class="col">
            <span class="fs-2 fw-bold">รายชื่อที่ได้ลงทะเบียน สำเร็จแล้ว</span>
            <% 
                if(status_txt == 1)
                {
            %>
            <br>
                <span class="fs-2 fw-bold text-success">ท่านได้ลงทะเบียนสำเร็จแล้ว</span>
            <%
                }
            %>
        </div>
    </div>
    <table class="table table-bordered text-center">
        <tr>
            <th>ลำดับ</th>
            <th>ชื่อ</th>
            <th>อายุ</th>
            <th>เบอร์โทร</th>
            <th>อีเมลล์</th>
        </tr>
        <% 
            if(status_txt == 1)
            {
                
                regs.forEach(regs => { 
        %>
            <tr>
                <td><%= regs.id %></td>
                <td><%= regs.name %></td>
                <td><%= regs.age %></td>
                <td><%= regs.phone %></td>
                <td><%= regs.email %></td>
            </tr>
        <% 
                });
            }
            else
            {
        %>
            <tr>
                <td colspan="5" class="text-danger fs-3 fw-bold">ไม่สามารถแสดงข้อมูลได้ กรุณาลงทะเบียนอีกครั้ง</td>
            </tr>
        <%
            }
         %>
    </table>

    <div class="row p-4">
        <div class="col">
            <a class="btn btn-primary" href="/">กลับ</a>
        </div>
    </div>
    </body>
</html>
